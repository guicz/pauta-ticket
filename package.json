{
  "name": "pauta-fluxo",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "deploy": "npm run build && firebase deploy --only hosting",
    "test": "vitest run --exclude functions/** --exclude functions-core/** --exclude scripts/**",
    "test:watch": "vitest --exclude functions/** --exclude functions-core/** --exclude scripts/**",
    "test:rules": "firebase emulators:exec --only firestore --project demo-pauta --config firebase.emulator.json \"node --test scripts/firestore-rules.test.mjs\"",
    "test:services": "npm test --prefix functions && npm test --prefix functions-core",
    "deploy:services": "npm run build && firebase deploy --config firebase.services.json --only functions,firestore,hosting"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "firebase": "^12.18.0",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "firebase-tools": "^15.29.0",
    "typescript": "latest",
    "vitest": "latest"
  }
}
